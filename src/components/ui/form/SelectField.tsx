import styled from "styled-components";
import { theme } from "../../../styles/theme";

export const SelectField = styled.p`
  display: flex;
  flex-direction: column;
  gap: ${theme.mobile.spacing.xxxs};
`;
